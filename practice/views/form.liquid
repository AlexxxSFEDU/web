<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Тестирование</title>
</head>
<body>
  <h1>Тестирование</h1>
  <form action="/submit" method="post">
    <div>
      <label for="name">Введите ваше имя:</label>
      <input type="text" id="name" name="name" required>
    </div>
    {% for question in questions %}
    <div>
      <label for="question{{ forloop.index }}">{{ forloop.index }}. {{ question }}</label>
      <input type="text" id="question{{ forloop.index }}" name="answer{{ forloop.index }}" required>
    </div>
    {% endfor %}
    <button type="submit">Отправить ответы</button>
  </form>
</body>
</html>
